const chai = require('chai');
const assert = chai.assert;

describe("azure module", () => {
  const azureModule = require('../azure.js');
  it("verifies that a user is authorized", () => {
      assert.strictEqual(true,false);
  });
});